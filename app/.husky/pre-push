#!/usr/bin/env sh
set -e

# core.hooksPath=app/.husky 이므로 레포 루트에서 실행된다. 아직 자동화된 테스트 스위트가
# 없어(스캐폴드 직후 단계) 빌드로 대체한다 — 테스트가 생기면 여기 `npm test`를 추가할 것.
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT/app"

echo "== pre-push: npm run build =="
npm run build
